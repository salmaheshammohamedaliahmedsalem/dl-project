# Checkpoints

The `.pth` checkpoint files are not committed as Git blobs because they exceed GitHub's 100 MB normal file limit.

They are uploaded to the GitHub Release named `best-model-artifacts`:

- `task1_best_model.pth`
- `task2_best_model.pth`

Local copies are in:

- `/Users/salmaheshamsalem/Desktop/DEEP_LEARNING_PROJECT/best model/checkpoints/task1_ovr256_epoch1_best_model.pth`
- `/Users/salmaheshamsalem/Desktop/DEEP_LEARNING_PROJECT/best model/checkpoints/task2_lge_saved_best_model.pth`
